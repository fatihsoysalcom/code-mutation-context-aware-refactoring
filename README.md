# code-mutation-context-aware-refactoring
This example demonstrates the core concept of 'mutating code content' by comparing a naive approach with a more context-aware one. It shows how a simple string replacement (analogous to a basic RAG system) can lead to incorrect code changes due to lack of semantic understanding. In contrast, a 'MemStrata-like' approach applies changes only within t
